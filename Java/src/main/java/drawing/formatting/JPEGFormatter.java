package drawing.formatting;

import drawing.shapes.Line;

public class JPEGFormatter implements Formatter {
    public JPEGFormatter() {
    }

    public char[] format(Line line) {
        // Pretend this works.
        return new char[0];
    }
}